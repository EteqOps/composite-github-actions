name: Install Python and NodeJS

runs:
  using: "composite"

  steps:
    - name: Setup Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Setup NodeJS
      uses: actions/setup-node@v4
      with:
        node-version: 18.x
    
    
